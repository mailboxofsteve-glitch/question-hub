

The user wants two new nullable TEXT columns on `public.nodes`: `layer1_bold` and `trigger_word`. No UI or other table changes.

Note: `src/integrations/supabase/types.ts` is auto-generated and will refresh automatically after the migration runs — I should not hand-edit it (per project rules), but the user explicitly asked. I'll respect the project rule and let it regenerate; I'll call this out in the plan so expectations are clear.

## Plan

### 1. Database migration
Add a single migration with two `ALTER TABLE` statements:

```sql
ALTER TABLE public.nodes ADD COLUMN IF NOT EXISTS layer1_bold TEXT;
ALTER TABLE public.nodes ADD COLUMN IF NOT EXISTS trigger_word TEXT;
```

Both columns:
- Type `TEXT`
- Nullable (no `NOT NULL`, no default)
- No impact on existing rows, RLS, indexes, or policies

### 2. TypeScript types
`src/integrations/supabase/types.ts` is auto-generated by the Supabase integration and is listed as a do-not-edit file in this project. After the migration runs, it will be regenerated automatically and `layer1_bold` and `trigger_word` will appear as `string | null` in the `nodes` Row / Insert / Update types — exactly the shape requested. No manual edit needed.

### 3. Out of scope (per your "DO NOT" list)
- No UI changes (NodeForm, NodeDetailContent, NodePreview, etc.)
- No changes to existing columns
- No changes to other tables, RLS, or edge functions (e.g. `admin-nodes` `buildSearchBlob` stays as-is)

### Files changed
| File | Change |
|------|--------|
| New migration SQL | `ALTER TABLE` adding `layer1_bold` and `trigger_word` |
| `src/integrations/supabase/types.ts` | Auto-regenerated by Supabase (no manual edit) |

